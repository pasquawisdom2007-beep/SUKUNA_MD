'use strict';

const { createPeakCommand } = require('../../utils/peakCommandFactory');

module.exports = createPeakCommand({
    "name": "posterforge",
    "title": "Posterforge",
    "mode": "ai"
});
